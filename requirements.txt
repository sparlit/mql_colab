# Core dependencies (required)
MetaTrader5>=5.0.45
pandas>=2.0.0
numpy>=1.24.0
flask>=3.0.0
requests>=2.31.0
psutil>=5.9.0

# ML dependencies
lightgbm>=4.0.0

# Performance dependencies (C-extensions)
orjson>=3.9.0           # 5-10x faster JSON serialization (C-extension)
numba>=0.59.0           # JIT compilation for hot loops (LLVM-based)

# Optional dependencies (uncomment as needed)
# scipy>=1.11.0          # quant_models.py, gpu_engine.py
# redis>=5.0.0           # cache_layer.py L3 cache, infrastructure.py cluster manager
# kafka-python>=2.0.0    # infrastructure.py Kafka producer
# cupy-cuda12x>=12.0.0   # gpu_engine.py GPU acceleration (NVIDIA CUDA)
